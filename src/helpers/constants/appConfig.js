const appConstants = {
  apiBaseURL: "http://127.0.0.1:5500",
  localStorage: {
    tokenKey: "accessToken",
    accessToken: "accessToken",
    refreshToken: "refreshToken",
  },
};

export { appConstants };
